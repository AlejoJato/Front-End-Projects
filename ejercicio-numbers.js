let alutra_cm = 170;

let alutra_mts = 1.69;

let peso_kgs = 72.450;

let altura_redondeo = alutra_mts.toPrecision(2);

let peso_redondeo = peso_kgs.toFixed();

let igualdad = (Number.MAX_VALUE + 1) === Number.MAX_VALUE;